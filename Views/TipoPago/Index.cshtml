@model IEnumerable<MorfeoWeb.Models.TipoPago>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoPago1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoPago1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoPago">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoPago">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoPago">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
