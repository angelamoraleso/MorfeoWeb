@model IEnumerable<MorfeoWeb.Models.AsignarTurno>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEmpleadoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTurnoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdEmpleadoNavigation.IdEmpleado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTurnoNavigation.IdTurno)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAsignacionTurno">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAsignacionTurno">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAsignacionTurno">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
