@model MorfeoWeb.Models.AsignarHabitacion

@{
    ViewData["Title"] = "Editar Asignación";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-dark text-white py-3">
                    <h4 class="card-title mb-0">Modificar Asignación</h4>
                </div>
                <div class="card-body p-4">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
                        <input type="hidden" asp-for="IdAsignacion" />

                        <div class="mb-3">
                            <label asp-for="IdHuesped" class="form-label fw-bold text-secondary">Huésped</label>
                            <select asp-for="IdHuesped" class="form-select" asp-items="ViewBag.IdHuesped"></select>
                            <span asp-validation-for="IdHuesped" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="IdReserva" class="form-label fw-bold text-secondary">Reserva</label>
                            <select asp-for="IdReserva" class="form-select" asp-items="ViewBag.IdReserva"></select>
                            <span asp-validation-for="IdReserva" class="text-danger small"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="IdHabitacion" class="form-label fw-bold text-secondary">Habitación</label>
                            <select asp-for="IdHabitacion" class="form-select" asp-items="ViewBag.IdHabitacion"></select>
                            <span asp-validation-for="IdHabitacion" class="text-danger small"></span>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg shadow-sm">
                                Actualizar Asignación
                            </button>
                            <a asp-action="Index" class="btn btn-link text-muted">Cancelar y regresar</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}